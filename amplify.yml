version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 18.17.0
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: out
    files:
      - "**/*"
  cache:
    paths:
      - "node_modules/**/*"
