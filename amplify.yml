version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20.9.0
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: out
    files:
      - "**/*"
  cache:
    paths:
      - "node_modules/**/*"
